# ============================================================================
# Pi Remote Access Stack — Environment Variables
# ============================================================================
# Copy this file to .env and edit the values:
#   cp .env.example .env && nano .env
# ============================================================================

# --- Timezone ---
TZ=Asia/Riyadh

# --- Raspberry Pi Host IP (for cloudflared routing) ---
HOST_IP=192.168.1.100

# --- Home LAN Subnet (for Tailscale subnet routing) ---
# This allows your work laptop to access all services on the LAN
LAN_SUBNET=192.168.1.0/24

# ============================================================================
# TAILSCALE
# ============================================================================
# Generate an auth key at: https://login.tailscale.com/admin/settings/keys
#   - Type: Reusable (recommended for headless Pi)
#   - Expiry: 90 days or No expiry
#   - Tags: tag:pi (optional, for ACL policies)
# ============================================================================
TAILSCALE_AUTHKEY=tskey-auth-XXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
TAILSCALE_HOSTNAME=pi5

# ============================================================================
# CLOUDFLARE TUNNEL
# ============================================================================
# Create a tunnel at: https://one.dash.cloudflare.com → Networks → Tunnels
#   1. Click "Create a tunnel"
#   2. Choose "Cloudflared" connector
#   3. Name it: pi5-tunnel
#   4. Copy the tunnel token
#
# Or via CLI:
#   cloudflared tunnel login
#   cloudflared tunnel create pi5-tunnel
#   cloudflared tunnel token pi5-tunnel
# ============================================================================
CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoiXXXXWOUR_TUNNEL_TOKEN_HEREXXXXXX

# --- Domain (managed in Cloudflare DNS) ---
CLOUDFLARE_DOMAIN=example.com

# --- Subdomain Mappings ---
# These map to the ingress rules in cloudflared/config.yml
# Adjust the HOST_IP and ports to match your stack configuration
#
# cloud.example.com     → Nextcloud      (HOST_IP:8443)
# dash.example.com      → Homepage       (HOST_IP:3010)
# status.example.com    → Uptime Kuma    (HOST_IP:3001)
# pihole.example.com    → Pi-hole        (HOST_IP:8080)
# yt.example.com        → n8n YouTube    (HOST_IP:5678)
# tt.example.com        → n8n TikTok     (HOST_IP:5679)
# ig.example.com        → n8n Instagram  (HOST_IP:5680)
# x.example.com         → n8n X/Twitter  (HOST_IP:5681)
